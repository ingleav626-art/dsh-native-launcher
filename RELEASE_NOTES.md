# dsh-native-launcher v0.4.1 — 修复启动缓慢与通知设置未生效

本次为修复版本，解决两项长期存在的使用问题，并优化启动流程耗时。未引入新功能，不影响现有配置与数据。

## 重点修复

### 一、启动缓慢：快捷方式启动耗时 20~40 秒

**问题描述**

通过桌面快捷方式启动时，页面需等待 20~40 秒方可显示；在同一台设备上改用命令行启动，耗时约为 8 秒。该差异可稳定复现，与设备负载、启动次数无关。

**原因**

快捷方式采用隐藏窗口的方式启动服务，以避免启动过程出现命令行窗口。系统会将没有可见窗口的进程识别为后台任务，并在其启动约 3 秒后限制其处理器与磁盘性能（实测处理器性能约为正常水平的 1/3，磁盘读取速度约为 1/5）。服务的启动过程以文件加载为主，因此耗时被显著放大。命令行启动时窗口可见，不受该限制影响。

**解决方案**

插件在服务加载过程中将该进程的调度优先级提升至"高于正常"，系统不再按后台任务限制其性能。

**改善结果**

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 服务加载耗时 | 32.4 秒 | 6.0 秒 |
| 双击至页面显示 | 30~40 秒 | 约 10 秒 |

浏览器首次启动时需额外数秒，属浏览器自身启动耗时。同一性能限制在启动完成后仍会持续生效，因此本项修复对服务运行期间同样有效。窗口隐藏行为与启动方式均保持不变。

### 二、通知设置未生效：页面在前台时仍弹出提醒

**问题描述**

设置"仅在任务不在眼前时通知"后，当用户正在查看页面时，任务完成、等待批准或等待回答仍会弹出通知。

**原因**

页面仅在查看状态发生变化时（切换窗口、切换会话）上报一次状态，而后台按"15 秒内是否收到上报"进行判断。因此页面持续处于前台超过 15 秒后，后台即视为用户已离开。该问题因此表现为间歇性生效。

**解决方案**

页面处于前台时每 5 秒刷新一次查看状态。页面在前台时不发送提醒；切换至后台或关闭页面时，按原有逻辑正常提醒。

## 优化

**启动检测耗时**：启动前的服务存活检测在服务未运行时需等待约 2 秒。现改为先行判断端口监听状态，未监听到则直接启动。实测该环节耗时由 2052 毫秒降至 33 毫秒；检测结果与原有逻辑保持一致，不会出现误判为"服务未运行"的情况。

## 改进

- 版本探测：未设置 `DSH_HOME` 时正确回退至默认目录，不再返回空值
- 环境诊断：改用解析后的配置输出，避免日志与实际启动链路不一致
- 环境诊断：新增进程优先级记录，便于后续定位同类问题

## 其他调整

- **启动方式调整**：启动入口由批处理脚本（`.cmd`）改为 PowerShell 脚本（`.ps1`）。启动流程与行为逻辑不变——仍是"先检测服务是否已在运行：已在运行则唤起窗口，未运行则启动服务"，窗口仍为完全隐藏，快捷方式、托盘、自动打开页面均不受影响。调整目的是让脚本语法错误在发布前就能被发现（批处理脚本缺少官方语法校验器，错误只能在运行时暴露）。
- 启动脚本补充编码标记，避免特定系统环境下中文内容被错误解析而导致脚本失效

## 升级说明

更新插件后重启 DeepSeek 即可，托盘与快捷方式将自动重建，无需调整任何配置。

- 重启后的本次启动即已生效（优先级提升在服务加载过程中完成）
- 启动检测优化在启动脚本更新后生效，即自下一次启动起

## 兼容性

- **版本基准**：本版起以 **dsh 0.1.5-rc.2**（当前官方最新版）为适配与验证基准，不再针对更低版本做适配与验证；低于该版本时，启动日志会提示升级。需要更低版本 dsh 的用户，请安装对应的历史插件版本。
- 本次更新不涉及 dsh 内部接口变更；未引入新依赖，不涉及注册表变更，窗口行为保持不变
- 托盘、快捷方式、PWA、关窗即退及通知设置均不受影响
