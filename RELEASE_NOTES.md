# dsh-native-launcher v0.4.0

**这个版本的重点是一次完整的架构重写，而不是新功能。**

## 架构重写

- 全部代码从单文件迁移到 **TypeScript 严格模式**工程，esbuild 构建，产物按职责拆分（宿主 / 模块 / client 各自独立）
- 插件功能改为**模块容器**接入：每个功能模块是独立的 manifest + 工厂，可单独禁用、单独替换——为后续"分包独立迭代"打底（官方在快速完善自身功能，被替代的模块可以直接下线，不影响启动器本体）
- 启动脚本、托盘、快捷方式链路建立了**自动化守卫**（语法解析 / 函数引用完整性 / 行为级测试），并内置启动耗时埋点与诊断日志

## 新功能

- **任务通知卡片点击唤起**：点击通知卡片，唤起已有的 DeepSeek 窗口（与双击快捷方式同链路，不重复开页面）。需要网页已"安装为应用"（PWA）。
- **开机自启动**（默认关闭）：设置页开启后，开机自动运行 DSH WebUI。改动即时生效，卸载时自动清理。

## 修复

- 双击快捷方式无法唤起窗口：服务已运行时可能被误判为"服务不在"，已修复。
- 快捷方式启动后，浏览器自动打开可能静默失败，已修复。

## 调整

- 设置页移除与通知设置卡片重复的开关；"任务托盘通知"更名为"托盘弹系统通知"（总开关语义）。
- 升级后自动清理旧版本遗留的注册表项与脚本文件。

## 升级方式

更新插件后重启 DeepSeek（dsh）即可，托盘、快捷方式等自动重建，无需手动处理。
